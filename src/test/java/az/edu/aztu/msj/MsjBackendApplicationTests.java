package az.edu.aztu.msj;

import org.junit.jupiter.api.Test;

/** Context-load tests require the full environment; run them in CI with env vars set. */
class MsjBackendApplicationTests {
    @Test
    void placeholder() {
        // intentionally trivial so `gradle build` doesn't require a live DB
    }
}
